<!DOCTYPE html>
<html lang="fa" dir="rtl">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>MarketLive🚀</title>
  <link href="https://cdn.jsdelivr.net/gh/rastikerdar/vazirmatn@v34.0.0/dist/font-face.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/index.css">
</head>

<body>

  <header class="header glass">
    <div class="brand">
      <div class="logo">📈</div>
      <div class="title">
        <div class="name">MarketLive</div>
        <% switch (action) { case "gold" : %>
          <div class="sub">قیمت لحظه‌ای طلاها</div>
          <% break; case "currency" : %>
            <div class="sub">قیمت لحظه‌ای ارزها</div>
            <% break; case "crypto" : %>
              <div class="sub">قیمت لحظه‌ای کریپتو ها</div>
              <% break; case "dashboard" : %>
                <div class="sub">داشبورد لحظه‌ای بازارها</div>
                <% break; case "favorites" : %>
                  <div class="sub">مورد علاقه های شما</div>
                  <% break; } %>
      </div>
    </div>
    <nav class="header-actions">
      <%if(isLogin){%>
        <button class="btn small login-btn">خروج</button>
        <%}else{%>
          <button class="btn small login-btn">ورود</button>
          <%}%>
    </nav>
  </header>

  <section class="hero">
    <h1>آخرین قیمت‌ها</h1>
    <p class="muted">داده ها هر دقیقه به روزرسانی میشوند⌛</p>
  </section>

  <main class="grid" id="cardsGrid">
    <%assets.forEach(asset=>{%>
      <article class="card" data-symbol="<%=asset.symbol%>" tabindex="0">
        <div class="card-top">
          <div class="meta">
            <div class="icon">$</div>
            <div class="label">
              <div class="name">
                <%=asset.title%>
              </div>
              <div class="small muted">
                <%=asset.enTitle%>
              </div>
            </div>
          </div>
          <div class="action">
            <%if(asset.changePercent.startsWith("-")){%>
              <span class="tag down">
                <%=asset.changePercent?.split("-")[1]%> -
              </span>
              <%}else{%>
                <span class="tag up">
                  <%=asset.changePercent%>
                </span>
                <%}%>
                  <%if(asset.price2){%>
                    <%if(asset.price2.startsWith("-")){%>
                      <span class="tagValue down">
                        <%=asset.price2?.split("-")[1]%> -
                      </span>
                      <%}else{%>
                        <span class="tagValue up">
                          <%=asset.price2%>
                        </span>
                        <%}%>
                          <%}else{%>
                            <%if(asset.changeValue.startsWith("-")){%>
                              <span class="tagValue down">
                                <%=asset.changeValue?.split("-")[1]%> -
                              </span>
                              <%}else{%>
                                <span class="tagValue up">
                                  <%=asset.changeValue%>
                                </span>
                                <%}%>
                                  <%}%>
          </div>
        </div>
        <div class="card-body">
          <div class="price-row">
            <div class="price">
              <%if(asset.price1){%>
                <%=asset.price1%>
                  <%}else{%>
                    <%=asset.price%>
                      <%}%>
            </div>
            <div class="currency">تومان</div>
          </div>
          <div class="card-footer">
            <button class="btn ghost small open-details">جزئیات</button>
            <!-- <div class="trend muted">آخرین: ۵ ثانیه پیش</div>  -->
            <%if(asset.isFav){%>
              <button class="fav-btn active" title="افزودن به علاقه‌مندی">
                ☆
              </button>
              <%}else{%>
                <button class="fav-btn" title="افزودن به علاقه‌مندی">
                  ☆
                </button>
                <%}%>
          </div>
        </div>
      </article>
      <%})%>
  </main>

  <form method="get">
    <nav class="bottom-nav" id="bottomNav">
      <% switch (action) { case "gold" : %>
        <button class="bn-item" formaction="/p">📊 داشبورد</button>
        <button class="bn-item" formaction="/p/currency">💲 ارز</button>
        <button class="bn-item active">🪙 طلا</button>
        <button class="bn-item" formaction="/p/crypto">💎 کریپتو</button>
        <button class="bn-item" formaction="/p/favorites">⭐ مورد علاقه ها</button>
        <% break; case "currency" : %>
          <button class="bn-item" formaction="/p">📊 داشبورد</button>
          <button class="bn-item active">💲 ارز</button>
          <button class="bn-item" formaction="/p/gold">🪙 طلا</button>
          <button class="bn-item" formaction="/p/crypto">💎 کریپتو</button>
          <button class="bn-item" formaction="/p/favorites">⭐ مورد علاقه ها</button>
          <% break; case "crypto" : %>
            <button class="bn-item" formaction="/p">📊 داشبورد</button>
            <button class="bn-item" formaction="/p/currency">💲 ارز</button>
            <button class="bn-item" formaction="/p/gold">🪙 طلا</button>
            <button class="bn-item active">💎 کریپتو</button>
            <button class="bn-item" formaction="/p/favorites">⭐ مورد علاقه ها</button>
            <% break; case "dashboard" : %>
              <button class="bn-item active">📊 داشبورد</button>
              <button class="bn-item" formaction="/p/currency">💲 ارز</button>
              <button class="bn-item" formaction="/p/gold">🪙 طلا</button>
              <button class="bn-item" formaction="/p/crypto">💎 کریپتو</button>
              <button class="bn-item" formaction="/p/favorites">⭐ مورد علاقه ها</button>
              <% break; case "favorites" : %>
                <button class="bn-item" formaction="/p">📊 داشبورد</button>
                <button class="bn-item" formaction="/p/currency">💲 ارز</button>
                <button class="bn-item" formaction="/p/gold">🪙 طلا</button>
                <button class="bn-item" formaction="/p/crypto">💎 کریپتو</button>
                <button class="bn-item active">⭐ مورد علاقه ها</button>
                <% break; } %>
    </nav>
  </form>

  <div id="detailModal" class="modal hidden">
    <div class="modal-panel glass">
      <button class="modal-close" id="modalClose">✕</button>
      <div class="modal-content">
        <div class="modal-head">
          <div class="m-icon">📈</div>
          <div class="m-title">
            <h3 id="modalTitle">عنوان</h3>
            <p id="modalSubtitle" class="muted">جزئیات بیشتر</p>
          </div>
        </div>
        <div class="modal-stats">
          <div class="stat">
            <div class="label muted">قیمت فعلی</div>
            <div id="modalPrice" class="big">---</div>
          </div>
          <div class="stat">
            <div class="label muted">درصد تغییر</div>
            <div id="modalChange">---</div>
          </div>
          <div class="stat">
            <div class="label muted">قیمت تغییر</div>
            <div id="modalChangeValue">---</div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script src="/js/index.js"></script>
</body>

</html>